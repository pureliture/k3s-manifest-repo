apiVersion: apps/v1
kind: Deployment
metadata:
  name: tripick-frontend
  namespace: default
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tripick-frontend
  template:
    metadata:
      labels:
        app: tripick-frontend
    spec:
      containers:
      - name: tripick-frontend
        image: tkdgur1756/tri-pick:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 80
