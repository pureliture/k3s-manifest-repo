apiVersion: v1
kind: Service
metadata:
  name: tripick-frontend
spec:
  selector:
    app.kubernetes.io/name: tripick-frontend
  type: NodePort
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
