apiVersion: apps/v1
kind: Deployment
metadata:
  name: tripick-frontend
  namespace: default
  labels:
    env: dev
spec:
  selector:
    matchLabels:
      app: tripick-frontend
  template:
    metadata:
      labels:
        app: tripick-frontend
