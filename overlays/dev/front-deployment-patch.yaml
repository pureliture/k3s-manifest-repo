apiVersion: apps/v1
kind: Deployment
metadata:
  name: tripick-frontend
  namespace: default
  labels:
    app: tripick-frontend
spec:
  selector:
    matchLabels:
      app: tripick-frontend
  template:
    metadata:
      labels:
        app: tripick-frontend
